#include <headers/int_vec.h>
#include <stdio.h>
#include <ctype.h>

char* postfix() {
    char c;
    int_vec opstack = init();
    do {
        c = getchar();
        if (isalpha(c)) {

        }
    } while (c != '\n');
}

int main() {
    
}